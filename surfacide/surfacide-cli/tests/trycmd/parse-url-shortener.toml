bin.name = "surfacide"
args = ["parse", "../examples/url-shortener"]
status.code = 0

stdout = """
../examples/url-shortener/scenarios.surf: parsed OK (module `UrlShortener`, 3 decl(s))
../examples/url-shortener/substrate.surf: parsed OK (module `UrlShortener`, 1 decl(s))
../examples/url-shortener/surface.surf: parsed OK (module `UrlShortener`, 11 decl(s))
"""
